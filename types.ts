export interface SectionProps {
  id: string;
}

export interface NavItem {
  label: string;
  href: string;
}

export interface ContactFormInputs {
  name: string;
  contact: string;
  message: string;
}